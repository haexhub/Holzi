# Session-Start-Prompt — 2026-05-25

Copy-paste das Ganze in die neue Session.

---

Lies kurz `docs/plans/2026-05-25-next-session.md` für den Kontext und `docs/plans/SESSION_HANDOFF.md` falls die Hintergründe nicht schnell genug klar sind.

**Pre-flight**:

1. `git fetch --prune && gh pr view 23 --json state` — wenn `MERGED`: `git pull` auf `main` und weiter. Wenn `OPEN`: CodeRabbit triagen gegen `feedback-coderabbit-skip-patterns`, gültige Findings fixen, mergen, dann pullen.
2. `make up-local-full` falls Stack nicht läuft (Build ist cache-warm, dauert <60s).

**Aufgabe**:

Pick ein Item aus `docs/plans/2026-05-25-next-session.md` Section A/B/C/D/E und zieh es durch — empfohlene Reihenfolge ist im Plan. Wenn du dir nicht sicher bist welches: starte mit dem ersten Frontend-Quick-Win (Dark-mode toggle, Empty-State, oder bessere Error-Toasts) — alle ~30min lokal testbar.

**Workflow** (bekannt aus `feedback-pr-workflow-hermes`):

- `gh auth switch --user haexhub` falls Push 403 wirft
- CodeRabbit rate-limit 1/h auf der Orga, re-trigger mit `@coderabbitai review`
- Git-Config auf Martin Drechsel <mdrechsel@itemis.com>
- Keine Claude-Trailer in Commits, Conversation Deutsch / Code+Commits Englisch
- Memory-Files: lies sie wenn was unklar ist, schreib neue wenn du was Generisches lernst

**Was NICHT zu tun**:

- Versuch nicht claude `setup-token` oder OAuth-Token-Direct-Bearer gegen api.anthropic.com. Beide tot. Siehe `[[project-hermes-oauth-claude-2-1]]` für den Grund.
- claude-code Pin nicht versehentlich auf eine andere Version bumpen ohne den paste-prompt zu verifizieren (siehe gleiches Memory).

**Letzter Stand**: OAuth + Chat funktionieren end-to-end mit Claude Max. PRs #19, #20, #21, #22, #23 (offen), frontend #8 + #9.
