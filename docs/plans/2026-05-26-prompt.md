# Session-Start-Prompt — 2026-05-26

Copy-paste das Ganze in die neue Session.

---

Lies `docs/plans/2026-05-26-next-session.md` für den Kontext und `docs/plans/SESSION_HANDOFF.md` falls die Hintergründe nicht schnell genug klar sind. Memory-Files [[project-hermes-agent]], [[project-holzi-frontend]], [[project-holzi-deployment]], [[project-haex-claude-proxy-resolver-sqlite]] sind aktuell — pull dir die rein wenn du eine Komponente anfasst.

**Pre-flight** (3 Befehle):

1. `git fetch --prune` in `~/Projekte/Holzi`, `~/Projekte/holzi-frontend`, `~/Projekte/haex-claude-proxy-resolver-sqlite`, `~/Projekte/ansible`. Alles sauber, keine offenen PRs.
2. `curl -s https://holzi.haex.cloud/healthz` — muss `{"status":"ok"}` + 200 liefern.
3. Optional `make up-local-full` im `Holzi/` falls am Backend gearbeitet wird.

**Aufgabe (empfohlen)**:

**Phase 3: Telegram-Bot-Integration**. Schema ist schon da (`messenger_accounts.bot_username` + `bot_token_iv/tag/data` + `allowed_chat_ids`). Was fehlt: Routes + Worker + Frontend-Section. Plan im `docs/plans/2026-05-26-next-session.md` Section A.

Reihenfolge: Backend-PR (route + worker + lifecycle-rename + tests) → Frontend-PR (Token-Form + List-Section). Stacked oder parallel — Frontend kann gegen den lokalen Backend entwickelt werden.

Falls Telegram zu groß: pick ein Frontend-Quick-Win aus Section B (Auto-Title, Mobile-Layout, Streaming-Animation) oder eine Backend-Hygiene-Task aus Section C.

**Workflow** (bekannt aus [[feedback-pr-workflow-hermes]]):

- `gh auth switch --user haexhub` falls Push 403 wirft
- CodeRabbit rate-limit 1/h auf der Orga; re-trigger via `@coderabbitai review`
- Bei Squash-Merge-Konflikt (Branch + Merge-PR's Commit-SHA divergent): **PR schließen + fresh Branch aus main**, force-push ist vom auto-classifier geblockt
- Git-Config auf Martin Drechsel <mdrechsel@itemis.com>
- Keine Claude-Trailer in Commits, Conversation Deutsch / Code+Commits Englisch
- Memory-Files: lies sie wenn was unklar ist, schreib neue wenn was Generisches gelernt wird

**Was NICHT zu tun**:

- **WhatsApp** — Meta-ToS, kann das persönliche Konto bannen. Nicht implementieren.
- **Authentik vor holzi.haex.cloud** zurückbringen — wurde bewusst rückgebaut weil's den SPA-Bearer-Login doppeln würde.
- **Watchtower auf containrrr/watchtower** zurückschalten — der Repo ist archiviert, der `:latest`-Build hat einen Docker-API-Mismatch.
- **Force-Push auf PR-Branches** — auto-classifier blockt, und es ist unnötig wenn man die Squash-Merge-Konvention versteht.

**Letzter Stand**: alle drei Repos sauber auf main, Production-Stack auf haex.cloud läuft (hermes-server + holzi-claude-proxy + signal-cli-rest-api + holzi-frontend). Signal-Linking-UI ist drin aber noch nicht manuell end-to-end vom User getestet. Telegram-Schema ist vorbereitet, fehlt nur die Code-Implementation.
