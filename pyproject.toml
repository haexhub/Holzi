[project]
name = "hermes-server"
version = "0.0.1"
description = "Hermes — personal AI assistant backend (FastAPI, SQLite/FTS5, MCP, Signal worker)"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Marko Drechsel", email = "mdrechsel@itemis.com" }]

dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "structlog>=24.4",
    "aiosqlite>=0.20",
    "sqlalchemy[asyncio]>=2.0",
    "httpx>=0.27",
    "mcp>=1.0",
    "trafilatura>=1.12",
    "cryptography>=48.0.0",
    "croniter>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "asgi-lifespan>=2.1",
    "ruff>=0.7",
    "mypy>=1.13",
    "types-croniter>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hermes"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
# Integration tests (real Podman) are opt-in: there is no Podman daemon in CI.
# Run them explicitly with `pytest -m integration`.
addopts = "-m 'not integration'"
markers = [
    "integration: requires external services (e.g. a rootless Podman socket)",
]
